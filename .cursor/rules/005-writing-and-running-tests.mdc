---
description:
globs:
alwaysApply: false
---
1. Use Rove framework to define tests and to run them. Read Rove documentation here: https://github.com/fukamachi/rove
2. Before running tests, load the ASDF system like `current-system-tests` using MCP tool `eval_lisp_form`.
3. Run tests using MCP tool `eval_lisp_form`.